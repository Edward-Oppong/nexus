import React from 'react';
import { LandingPage } from '../../landing/LandingPage';

/**
 * LandingView - Public-facing product storytelling layer for Nexus.
 * Orchestrates the full clinical narrative experience (Sections 01 through 23).
 */
export const LandingView: React.FC = () => {
  return <LandingPage />;
};
